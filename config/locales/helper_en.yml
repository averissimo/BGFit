# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  help:
    experiment:
      tip: "tip: an experiment aggregates different measurements. You can use different experiments to aggregate identical conditions or if you wish to logically separate your results, otherwise just use the default experiment."
    group:
      tip: "tip: give write or read permissions to a team."
    