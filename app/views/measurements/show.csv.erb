[Name]
<%= @model.title + " - " + @experiment.title + " - " + @measurement.title + " (" + @measurement.date.to_s + ")" %>

[Notes]
<%= @model.description %>

[Components]
time,N

[Componentnotes]

[Values]
<%= @measurement.lines_no_death_phase.map { |l| l.x.to_s + "," + l.y.to_s }.join("\n").html_safe %>

