<% 
	context_menu = [
		{
			:key => :show, 
			:name => "Show model", 
			:url => model_path(@model),
			:options => {
				:container_class => 'menu',
				:class => 'text'
			}
		},
		{
			:key => :back, 
			:name => "Back to models' index", 
			:url => models_url,
			:options => {
				:container_class => 'menu',
				:class => 'text'
			}
		}]
	menu = nil
	content_for(:context_menu) { menu = render_navigation :items => context_menu }
	content_for(:title) { "Edit model" }
%><h1>Editing model</h1>

<%= render 'form' %>

<div class="menu">
	<%= menu %>
</div>

